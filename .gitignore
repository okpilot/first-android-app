# Secrets / environment — NEVER commit these
.env
.env.*
!.env.example
*.pem
*.key

# OS & editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.iml
*.swp

# Flutter / Dart build artifacts
.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
/coverage/
migrate_working_dir/
.pub-cache/
.pub/
**/doc/api/
app.*.symbols
app.*.map.json
**/ios/Flutter/.last_build_id
# Keep committed: pubspec.lock, .metadata

# Local dev backend secrets & Flutter dart-defines (never commit)
backend/.env
dev-defines*.json

# Stray design/export archives dropped in the project root (e.g. logo zips)
*.zip
