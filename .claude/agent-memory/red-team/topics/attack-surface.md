# Attack-surface matrix — red-team (First Android App)

> **Protected topic file — never pruned, never inlined into MEMORY.md.** The threat-vector →
> coverage matrix. Update it in place after every red-team review: add new vectors the diff
> introduces, update `Covered by` when a check is recommended/lands, and transition `Status`.
> `Status` legend: `INFO baseline` (intended posture — single-user, tailnet-only, no login per D37) ·
> `gap` (reachable, no check) · `partial (static only)` (a static check exists but no runtime
> curl/widget/integration one) · `covered` (a curl/widget/integration check exists) · `CLOSED (D36)`
> (a former pre-auth gap now hardened) · `N/A (no auth, D37)` (moot without login — flips live only if
> the no-auth decision is revisited: shared / publicly exposed / multi-tenant).

## Current vectors (single-user, tailnet-only, no login — Decision 37)

| Vector | Surface | Covered by | Status |
|---|---|---|---|
| anon reads over live rows (WRITES CLOSED) | `anon` can still SELECT any non-deleted row via PostgREST (reads stay direct-under-RLS — expected pre-auth). **Direct writes CLOSED by Decision 36 (`20260715120000`):** `revoke insert, update … from anon, authenticated` + drop of the `_insert`/`_update` policies on ALL 5 mutable tables (contacts, event_types, event_comments, tasks, task_comments) → the SECURITY DEFINER RPCs are the SOLE write path (matching events/event_attendees). Verified no residual write grant survives — every original grant was `to anon, authenticated` (no PUBLIC/other-role table grant to leak past the revoke); SELECT grant + all 5 `_select` policies untouched, so reads are unaffected. Never had a delete grant | Direct-insert-rejected (401/403, was 201) + RPC-write-still-works + read-still-200 curls in `backend/README.md` ("Verify: pre-auth lockdown") — contacts shown as the representative table | **covered / writes CLOSED.** Reads remain INFO pre-auth (#3). A NEW table that re-opens a direct anon write path (insert/update grant or direct write policy) is now a REAL finding, not baseline (ref D36) |
| RPC EXECUTE to PUBLIC (CLOSED) | Postgres default `EXECUTE` to PUBLIC on SECURITY DEFINER RPCs. **CLOSED by Decision 36 (`20260715120000`):** `revoke execute … from public` on all 21 client-facing RPCs, each at its latest/only signature. **Verified drop-clean:** the 4 re-signatured chains (create_task/update_task via notes+contacts, create_event/update_event via add-type) each `drop function if exists <old sig>` before the recreate → NO orphan overload keeps PUBLIC execute; revoke list = exactly the 21 distinct granted RPCs (none omitted; pgrst_watch correctly excluded — not SECURITY DEFINER / not client-facing). Explicit anon/authenticated grants persist → app unaffected | Static `revoke` present + fresh-pg verify in commit msg. **No runtime/privileged check recorded** that PUBLIC actually lost execute — an anon curl CANNOT prove it (anon calls via its own explicit grant). Nil real exposure today: no client login role falls back to PUBLIC-only in the trimmed stack | **CLOSED (static)** — `db-security-reviewer` owns the static gate check. Runtime proof is a minor coverage gap → recommend a privileged psql check (below). A NEW RPC that reintroduces the PUBLIC grant is now a REAL finding (ref D36) |
| Soft-delete → hard-delete | Three `soft_delete_*` RPCs live (contact, event, event_type) — all `update … set deleted_at = now() where … deleted_at is null`, no `DELETE`. Must flag, not erase | soft-delete-doesn't-hard-delete curl: after RPC, `select … where id=…` still returns the row with `deleted_at` set — **NOT yet written** (backend/README.md has only a contacts `select` curl) | **gap** — recommend the survives-with-deleted_at curl for all three RPCs |
| Soft-deleted type → dangling embed | Soft-deleting an `event_type` must make `events?select=…,event_types(…)` embed `null` (type_id FK has NO on-delete; row hidden by SELECT policy, not gone) | Client tolerance **covered** by `test/event_test.dart` (null embed → `Event.type == null`, lines 104-122; null contact embed skipped, lines 54-63). DB-layer producing the null: **NOT** documented by curl | **partial** — client covered; recommend the DB embed-returns-null curl |
| RLS present on new table | A new `create table` reachable by `anon` without RLS = unfiltered exposure | `db-security-reviewer` static check; red-team confirms the *surface* | **partial (static only)** — RLS presence is verified statically; no anon-RLS *runtime* curl exists yet (`covered` in this matrix means a curl/widget/integration check) |
| Cross-user data access | User A reads/mutates user B's rows | — | **N/A (no auth, D37)** — single-user, no login; nothing to cross. Flips to CRITICAL/ISSUE only IF the no-auth decision is revisited (shared / public / multi-tenant) |
| Owner-scoping | RPC/policy scopes rows to the caller (`auth.uid()`) | — | **N/A (no auth, D37)** — WON'T-DO. Flips to ISSUE only IF the no-auth decision is revisited |
| event_attendees hard-DELETE via update_event | `update_event` runs `delete from event_attendees where event_id = p_id` to replace the set (annotated exception, database.md #4 — membership is derived). Guarded: bails with `no_data_found` if the event is unknown/soft-deleted, so a hidden event's set can't be silently rewritten | The `if not found` guard is the control; no curl asserts it. Pre-auth anon can already call it | INFO pre-auth (#3) — intended design + guarded; not an attack today |
| event_comments SELECT `using(true)` (archived readable) | Deliberate divergence: SELECT policy is `using(true)`, so archived (`deleted_at IS NOT NULL`) rows stay readable — that IS the "Show archived" feature. Columns are id/event_id/body/timestamps only — NO author/identity/auth column, so no extra sensitive exposure beyond comment bodies vs the anon-CRUD baseline | Widget round-trip `test/comments_section_test.dart`; live curl run in commit 3a87cc8 (archived row still SELECTable) but NOT recorded in backend/README.md | INFO pre-auth (#3). **Phase-flip watch:** post-auth, `using(true)` + no author column means once an author col is added a user could read another user's archived comments — revisit owner-scoping for this table when auth lands |
| event_comments write path via RPCs (`create/update/soft_delete/restore_comment`, `20260712150000`, Decision 26 Slice 3) — direct-write policies **DROPPED by D36 (`20260715120000`)** | App writes comments through the 4 RPCs, which are now the **SOLE** write path: D36 revoked the `insert,update` grants + dropped the `event_comments_insert`/`event_comments_update` policies (for anon AND authenticated). A raw anon/authenticated client can no longer mutate a comment directly. Non-destructive: NO delete grant on the table, no `DELETE` in any RPC, body CHECK `length(trim(body))>0` blocks blanking on both paths | This is the ONE table where a plain anon `select … where id=` PROVES soft-delete persistence (archived rows visible under `using(true)`, unlike RPC tables filtered by `deleted_at is null`). Widget round-trip `test/comments_section_test.dart`; archived-persists curl now **RECORDED in `backend/README.md`** ("Verify: event comment write RPCs" — the `soft_delete_comment` step's `select … where id=` shows the row with `deleted_at` non-null) | **covered** — the persistence curl is durable in `backend/README.md`. INFO pre-auth (#3) |
| comment write RPCs `create/update/soft_delete/restore_comment` (SECURITY DEFINER, `20260712150000`) | 4 new RPCs, uniformity refactor (Decision 26 Slice 3). `search_path=public` pinned on all four (:29/:52/:76/:98); bodies parameterized static INSERT/UPDATE with `trim(p_body)` — NO dynamic SQL, no injection. `create_comment` fires the body CHECK (`length(trim(body))>0`) + `event_id` FK (`on delete restrict`) on a bad arg. `update_comment` & `soft_delete_comment` guard `where id=p_id and deleted_at is null` + `raise no_data_found`: cannot edit/re-archive a hidden or absent row. **KEY NUANCE:** unlike the contact/event_type/event soft-delete RPCs, these do NOT exist to dodge 42501 — event_comments' SELECT is `using(true)`, so a direct write already worked; RPCs are for write-path uniformity (one future home for `auth.uid()`). RPCs are for write-path uniformity. **Post-D36 the RPCs are the sole write path** (direct write policies dropped), so `restore_comment`'s un-delete (`set deleted_at=null where deleted_at is not null`) is now reachable ONLY via the RPC, not a raw anon UPDATE. | No hard-delete path (no `DELETE`, no cascade touched). Static hygiene → `db-security-reviewer`. Soft-delete persistence provable via the same plain-anon-`select` curl as the direct-UPDATE row above (archived row survives with `deleted_at` set). The `soft_delete_comment`→`restore_comment` round-trip and the `update_comment`-refuses-archived curl (soft_delete a comment → update_comment it → expect `no_data_found`) are now **RECORDED in `backend/README.md`** ("Verify: event comment write RPCs"), alongside the contact/event_type RPC curls | **covered** — the four-RPC curls are durable in `backend/README.md`. INFO pre-auth (#3) — correctness, not an attack. **Phase-flip watch (auth #3):** these RPCs bypass RLS (SECURITY DEFINER), so when #3 owner-scopes event_comments the `auth.uid()` owner check MUST go INSIDE all four — and `restore_comment` especially, since an un-delete of another user's archived comment is a distinct write. Also revisit `using(true)` SELECT (row above) — post-auth an author col would let a user read/restore another user's comments |
| event_comments `on delete cascade` from events | FK `event_id … on delete cascade` is a hard-delete path for comments. Dormant today: events are NEVER hard-deleted (verified — no `delete from events` anywhere; `soft_delete_event` only sets `deleted_at`), so the cascade never fires | Guarded implicitly by the events soft-delete-only invariant (database.md #4). No direct check | INFO pre-auth (#3) / **latent-coupling watch** — if a hard DELETE of events is ever introduced, comments would be silently hard-deleted, violating soft-delete-only. Keep "events are never hard-deleted" as the control |
| contact write RPCs `create_contact` / `update_contact` (SECURITY DEFINER) | New write path added `20260712130000` (Decision 26). `search_path=public` pinned; bodies are parameterized static INSERT/UPDATE with `trim`/`nullif` — NO dynamic SQL, no injection. `update_contact` guards `where id=p_id and deleted_at is null` + `raise no_data_found` if `not found`: cannot mutate/resurrect a soft-deleted or absent row (never writes `deleted_at`, WHERE excludes hidden rows). No net new surface pre-auth — create/update touch only non-delete fields (deleted_at stays null), so a direct write under the still-present `contacts_insert`/`contacts_update` policies would do the same. Name CHECK `length(trim(name))>0` fires through the RPC (blank/NULL name → check/not-null violation, DB catches) | `update_contact`-refuses-soft-deleted curl (soft_delete_contact a row → update_contact it → expect `no_data_found`), plus create_contact happy-path (trim + empty→null) and blank-name-rejected curls, **RECORDED in `backend/README.md`** ("Verify: contact write RPCs", commit 13a0b3e); also exercised as the `anon` role on a fresh postgres:16 during the slice | **covered** — the three curls are durable in `backend/README.md`. Severity was INFO (correctness, not an attack pre-auth). **Phase-flip watch (auth #3):** these RPCs bypass RLS, so once #3 tightens contacts policies to owner-scoping, the `auth.uid()` owner check MUST be added *inside* create/update_contact or the RPC path lets a user write any row — this is the single spot the header comment flags |
| event_type write RPCs `create_event_type` / `update_event_type` (SECURITY DEFINER) | Near-clone of the contact write RPCs, added `20260712140000` (Decision 26, Slice 2). `search_path=public` pinned; bodies are parameterized static INSERT/UPDATE with `trim(p_name)` (color passed through as-is) — NO dynamic SQL, no injection. `update_event_type` guards `where id=p_id and deleted_at is null` + `raise no_data_found` if `not found`: cannot mutate/resurrect a soft-deleted or absent row (never writes `deleted_at`, WHERE excludes hidden rows). No net new surface pre-auth — create/update touch only name/color (deleted_at stays null), so a direct write under the still-present `event_types_insert`/`event_types_update` policies would do the same. Both table CHECKs (`length(trim(name))>0`, `color ~ '^#[0-9A-Fa-f]{6}$'`) re-fire through the RPC | `update_event_type`-refuses-soft-deleted curl (soft_delete → update → expect `no_data_found`), create happy-path (trim proven `"  Focus  "`→`Focus`), blank-name→400 and malformed-color→400 curls, **RECORDED in `backend/README.md`** ("Verify: event type write RPCs") — both table CHECKs (name + color) proven firing through the RPC | **covered** — the curls are durable in `backend/README.md`. Severity INFO (correctness, not an attack pre-auth). **Phase-flip watch (auth #3):** same as contacts — these RPCs bypass RLS, so when #3 owner-scopes event_types the `auth.uid()` owner check MUST go *inside* create/update_event_type or the RPC path lets a user write any type |
| tasks SELECT `using(true)` (archived readable) | 2nd viewable-soft-delete table (alongside event_comments, Decision 27). SELECT policy `using(true)`, so archived (`deleted_at IS NOT NULL`) tasks stay readable = the "Show archived" section. Columns are id/title/is_done/**notes** (added `20260714120000`)/timestamps/deleted_at only — still NO author/identity/owner column; `notes` is one more anon-readable/writable freeform text field, subsumed by the anon-CRUD baseline (no new exposure beyond `title`). `using(true)` on the UPDATE policy also lets an archived row be targeted to restore it | Live curl in `backend/README.md` ("Verify: task write RPCs") — `soft_delete_task` then `select …&select=id,is_done,notes,deleted_at` shows the row present WITH its `notes` readable and `deleted_at` non-null (archived-notes-readable proven — a note is set/edited before the archive step, not cleared) | **covered** — the persistence/archived-notes-readable curl is durable in `backend/README.md`. INFO baseline (single-user, no login). **Revisit-only-if-auth watch (D37):** `using(true)` reads are fine with one user; only IF the no-auth decision is revisited (shared/public/multi-tenant) would an owner col + owner-scoping be needed so a user can't read another user's archived tasks |
| tasks write RPCs `create_task`/`update_task`/`soft_delete_task`/`restore_task` (SECURITY DEFINER, `20260712160000`; `create_task`/`update_task` re-signatured `20260714120000` +notes, `20260714160000` +contacts, `20260715130000` +importance) | 4 RPCs (Decision 27). **Importance follow-on (`20260715130000`, Decision 38):** `alter table tasks add column importance smallint not null default 0 check (importance between 0 and 3)` + `create_task`/`update_task` re-signatured via `drop … ; create or replace …` to add `p_importance smallint` (create default 0; **update REQUIRED-no-default**, same footgun-guard as `p_contacts` — an omitted arg fails loudly PGRST202 instead of silently resetting importance to 0). **Drops match prior sigs EXACTLY** (`create_task(text,text,uuid[])`, `update_task(uuid,text,boolean,text,uuid[])`) → no orphan overload keeps PUBLIC execute; **D36 lockdown correctly re-honored** — migration re-issues BOTH `revoke execute … from public` AND `grant … to anon, authenticated` on the NEW `…,smallint` sigs (user verified on fresh DB: `has_function_privilege('public', …, 'execute')`=f, anon/authenticated=t). `p_importance` is a **typed smallint arg** (no dynamic SQL, no injection); out-of-range/wrong-type coerces to a check/type violation (400), can't corrupt or wrap. No new table grant/policy on tasks; writes stay RPC-only. Bodies otherwise the prior verbatim + the `importance` column. **Notes follow-on (`20260714120000`):** `create_task(text)`→`create_task(text, text default null)`, `update_task(uuid,text,boolean)`→`update_task(uuid,text,boolean,text)` via `drop … ; create or replace …` (signature-change convention). **Drops verified to exactly match the prior signatures** → no orphan overload, no PGRST203 ambiguity; grants re-issued on the NEW signatures (:86-87). `notes` handled `nullif(trim(p_notes),'')` — parameterized, NO dynamic SQL, no injection; `security definer` + `set search_path=public` retained in both recreated bodies (:32-33/:60-61); soft-delete path untouched, `update_task` still guards `where id=p_id and deleted_at is null` (can't edit an archived task's notes) and never writes `deleted_at`. No net new attack surface — `notes` is one more freeform field on the anon-CRUD baseline. `search_path=public` pinned on all four; bodies are parameterized static INSERT/UPDATE with `trim(p_title)` — NO dynamic SQL, no injection. `create_task` fires the table CHECK `length(trim(title))>0` on a blank arg. `update_task` guards `where id=p_id and deleted_at is null` + `raise no_data_found`: cannot edit/toggle a hidden or absent row, and never writes `deleted_at` (so no resurrect via update). `soft_delete_task` guards `deleted_at is null` (idempotent, no double-archive); `restore_task` is the inverse un-delete `set deleted_at=null where deleted_at is not null`. **KEY NUANCE (header-annotated):** unlike contact/event_type/event soft-delete RPCs, these do NOT dodge a 42501 — `using(true)` SELECT means a direct write already worked; the RPCs are write-path UNIFORMITY (one future home for `auth.uid()`). **Post-D36 the RPCs are the SOLE write path:** the `tasks_insert`/`tasks_update` direct policies + grants were dropped/revoked (anon AND authenticated) by `20260715120000`, so `restore_task`'s un-delete is now reachable only via the RPC, not a raw anon UPDATE | No hard-delete path: `grant select,insert,update` only (NO delete grant), no DELETE policy, no `DELETE` in any RPC. Full curl set RECORDED in `backend/README.md` ("Verify: task write RPCs"): create happy-path (trim proven `"  Buy milk  "`→`Buy milk`, is_done false, deleted_at null), update rename+done, soft_delete→archived-row-survives-with-`deleted_at`, update-refuses-archived→`no_data_found`/P0002, restore, blank-title→400, and hard-DELETE→401 (no delete grant). **Notes follow-on (now covered):** the block asserts the `p_notes` path — `create_task` `{"p_title":"x"}` (notes omitted → default null) documents the old `create_task(text)` overload is gone / no PGRST203; `create_task`/`update_task` with `p_notes:"  …  "`→ stored trimmed and `p_notes:"  "`→ stored NULL (empty/whitespace normalization, mirrors `title`); and a note survives the archive step to prove archived-notes-readable. **Importance follow-on (`20260715130000`) — RECORDED in `backend/README.md`** ("Verify: task importance"): (a) create/update with `p_importance:2` → reads back `importance:2`; (b) out-of-range `p_importance:5` (and `-1`) → check violation 400 (documents the DB guard); (c) the superuser `has_function_privilege('public','public.create_task(text,text,uuid[],smallint)','execute')`=`f` privileged check (an anon curl can't prove PUBLIC lost execute) | **covered** for the attack surface (soft-delete/hard-delete unchanged) AND the notes correctness path (curls durable in `backend/README.md`). INFO pre-auth (#3) — correctness, not an attack. **Phase-flip watch (auth #3):** these RPCs bypass RLS (SECURITY DEFINER), so when #3 owner-scopes `tasks` the `auth.uid()` owner check MUST go INSIDE all four — `restore_task` especially (un-delete of another user's archived task is a distinct write). Also revisit the `using(true)` SELECT row above |
| task_comments SELECT `using(true)` (archived readable) | 3rd viewable-soft-delete table (alongside event_comments + tasks). SELECT policy `using(true)`, so archived (`deleted_at IS NOT NULL`) comments stay readable = the "view archived" feature (Decision 33 / Slice 2b, `20260714140000`). Columns are id/task_id/body/timestamps/deleted_at only — NO author/identity/owner column, same as event_comments, so no task-comments-specific leak beyond comment bodies vs the anon-CRUD baseline. `using(true)` on UPDATE also lets an archived row be targeted to restore it | Live curl in `backend/README.md` ("Verify: task comment write RPCs") — `soft_delete_task_comment` then `select …&select=id,body,deleted_at` shows the row present WITH `deleted_at` non-null (archived-readable + soft-delete persistence proven; because `using(true)`, a plain anon `select` DOES prove persistence here, unlike RPC tables filtered by `deleted_at is null`). Widget round-trip `test/comments_section_test.dart` | **covered** — persistence curl durable in `backend/README.md`. INFO baseline (single-user, no login). **Revisit-only-if-auth watch (D37):** only IF the no-auth decision is revisited would an owner col + owner-scoping be needed so a user can't read another user's archived task comments |
| task_comments write RPCs `create_task_comment`/`update_task_comment`/`soft_delete_task_comment`/`restore_task_comment` (SECURITY DEFINER, `20260714140000`) | Task-side twin of the event_comment RPCs (Decision 33 / Slice 2b), born on the RPC path (no direct-write history to convert). **Faithful line-by-line clone of `20260712150000` — verified NO drift** (FK RESTRICT not cascade; RLS same migration; `search_path=public` pinned on all four; INSERT/UPDATE policies `with check(true)`; no DELETE grant/policy). Bodies are parameterized static INSERT/UPDATE with `trim(p_body)` — NO dynamic SQL, no injection. `create_task_comment` fires body CHECK `length(trim(body))>0` + `task_id` FK (`on delete restrict`) on a bad arg. `update_task_comment` (body only — no p_task_id, can't move a comment to another task) & `soft_delete_task_comment` guard `where id=p_id and deleted_at is null` + `raise no_data_found` (can't edit/re-archive a hidden/absent row → no double-archive). `restore_task_comment` inverse `set deleted_at=null where deleted_at is not null` (no double-restore). **KEY NUANCE:** RPCs are write-path UNIFORMITY. **Post-D36 (`20260715120000`) the RPCs are the SOLE write path** — the direct INSERT/UPDATE policies + grants were dropped/revoked (anon AND authenticated), and the 4 RPCs additionally guard the **parent task's** `deleted_at` (archived-task log frozen server-side). A raw anon/authenticated client can no longer mutate these comments directly | No hard-delete path (no DELETE grant/policy, no `DELETE` in any RPC, FK RESTRICT). Full curl set RECORDED in `backend/README.md` ("Verify: task comment write RPCs"): create+trim (`"  first note  "`→`first note`), edit, soft_delete→archived-row-survives-with-`deleted_at`, update-refuses-archived→`P0002`, restore, blank-body→400, hard-DELETE→401. Static hygiene → `db-security-reviewer` | **covered** — curls durable in `backend/README.md`. INFO pre-auth (#3) — correctness, not an attack. **Minor coverage nits (parity with event_comments, NOT new):** README block doesn't explicitly assert (a) restore CLEARS `deleted_at` back to null (runs restore but no follow-up select), (b) double-archive/double-restore `no_data_found`, (c) unknown `p_task_id`→FK 400. Guards proven by code symmetry; optional to add. **Phase-flip watch (auth #3):** SECURITY DEFINER bypasses RLS — when #3 owner-scopes `task_comments`, the `auth.uid()` owner check MUST go INSIDE all four, `restore_task_comment` especially (un-delete of another user's archived comment is a distinct write). Also revisit the `using(true)` SELECT row above |
| DDL-watch event triggers (`pgrst_ddl_watch`/`pgrst_drop_watch` → `public.pgrst_watch()`) | Two event triggers fire `NOTIFY pgrst, 'reload schema'` on any DDL (`20260712120000`). NOT attacker-reachable: event triggers fire ONLY on DDL, which anon/authenticated cannot run (no CREATE/ALTER/DROP via PostgREST — DML/RPC don't fire them). Function returns `event_trigger` → not exposable as an RPC and errors if invoked outside trigger context, so the usual PUBLIC-EXECUTE #3 concern is doubly moot. Channel/payload are fixed literals (no user data interpolated) → no NOTIFY spoof/injection; only listener is PostgREST, reload is idempotent. Not SECURITY DEFINER (runs in DDL executor's context — no elevation) + `search_path=''` (no CVE-2018-1058 surface). Postgres dedupes identical NOTIFY within a txn → a multi-statement migration = one reload, no self-storm | No attacker vector to cover. **Availability coupling** = the only real consideration: any future edit that makes `pgrst_watch()` fallible would abort EVERY DDL (incl. migrations). Recommend a one-time smoke check on homebase (as postgres): `psql -c "create table _z(); drop table _z;"` succeeds AND PostgREST picks up a new object without a manual reload | INFO pre-auth — no anon-reachable surface. **Latent-coupling watch:** keep `pgrst_watch()` body a bare NOTIFY; if it ever grows logic, re-review the DDL-abort blast radius |

| task_contacts join table + `p_contacts uuid[]` on create_task/update_task (SECURITY DEFINER, `20260714160000`) | New M2M `task_contacts(task_id, contact_id)` composite PK, both FKs `on delete cascade`; RLS on, **SELECT-only** grant/policy for anon (`using(true)`, NO write policy/grant) → membership set ONLY by the RPCs. `create_task`/`update_task` re-signatured to add `p_contacts` via `drop … ; create or replace …`. **Drops verified to match prior sigs exactly** (`create_task(text,text)`, `update_task(uuid,text,boolean,text)`) → no orphan overload, no PGRST203; grants re-issued on NEW sigs (:132-133). **CR follow-up `3f5359d`: `update_task.p_contacts` default `'{}'` DROPPED (now REQUIRED); `create_task` keeps its default.** This is a hardening, NOT a surface change — a Postgres default is not part of function identity, so the signature/grant/EXECUTE posture is untouched; the only behavioural delta is that an omitted `p_contacts` on update now fails loudly (PGRST202 function-not-found) instead of silently WIPING the whole People set. Removes a data-loss footgun; widens nothing. Create keeps the default (a create has no existing set to wipe). Membership INSERT is `insert … select v_id, c from unnest(p_contacts) as c on conflict do nothing` — **parameterized array, NO dynamic SQL, no injection**; `unnest(NULL)`/`unnest('{}')` = safe no-op; `on conflict do nothing` dedupes. `security definer` + `set search_path=public` on both bodies → search-path hijack (CVE-2018-1058) closed, siblings-consistent. **`p_contacts` accepts ANY valid contact uuid** — FK requires the contact to EXIST but NOT to belong to the caller. Pre-auth: no owners, and anon can read all contacts anyway → no new surface vs the event_attendees twin (same "link any contact_id" power). update_task's `delete from task_contacts where task_id=p_id` (replace-the-set) is a hard-DELETE of derived join rows = the annotated database.md #4 exception (mirrors update_event); **guarded** — the `update … deleted_at is null` + `if not found raise no_data_found` runs BEFORE the delete, so an unknown/archived task's roster can't be silently rewritten. SELECT is `using(true)` (NOT event_attendees' parent-live EXISTS gate) — deliberate: archived tasks stay readable so their roster must too; join row is only task_id/contact_id (no identity col). Cascade FKs are dormant (tasks & contacts are soft-delete-only, never hard-deleted) | Client round-trip covered: `test/task_form_screen_test.dart`, `test/task_detail_screen_test.dart`, `test/task_test.dart` (People parse/embed), `test/tasks_list_screen_test.dart`. **DB-layer join curl NOT recorded in `backend/README.md`** — no curl documents create_task with `p_contacts` inserting task_contacts rows, `on conflict` dedup, and the `tasks?select=…,task_contacts(contact_id,contacts(id,name,company))` embed reading back | **covered** (client) / DB-curl **gap**. INFO pre-auth (#3) — correctness, not an attack today. **PHASE-FLIP WATCH (auth #3) — strongest new item:** because `p_contacts` links ANY existing contact and the roster embeds `contacts(id,name,company)` under `using(true)`, once auth lands a user could link another user's contact to their own task and read that contact's PII via the embed → **cross-tenant contact disclosure**. When #3 owner-scopes, create/update_task MUST validate each `p_contacts` id belongs to `auth.uid()` (reject/filter foreign contacts), AND task_contacts SELECT must gate on the parent task's owner. Recommend then: integration/curl check that user A cannot link or read user B's contact via a task roster |

| **Pre-auth lockdown migration (`20260715120000`, Decision 36 / #3 auth-independent subset)** | ONE behavior-preserving migration, 3 parts: (1) close the direct anon write path on the 5 mutable tables; (2) `revoke execute … from public` on all 21 RPCs; (3) add a **parent-task-live guard** to the 4 task_comment RPCs. **Attack-surface verdict: opens NO new surface, no bypass found.** Part 1+2 covered in the two headline rows above. **Part 3 guard analysis:** `create_task_comment` now `raise no_data_found` unless the parent task exists AND `deleted_at is null` (FK on-delete-restrict already blocked unknown ids; the explicit `exists()` adds the archived-parent case). `update/soft_delete/restore_task_comment` add a correlated `exists(select 1 from tasks t where t.id = task_comments.task_id and t.deleted_at is null)` to their WHERE — correct correlation, `search_path=public` pinned, bodies are the prior verbatim + guard via `create or replace` (no drop → ACL preserved, so the Part-2 PUBLIC revoke survives). **No bypass:** the guard is the SOLE mutation path now that Part 1 dropped the direct write policies — pre-lockdown a raw anon direct UPDATE would have dodged it; post-lockdown there is no direct path left. `restore` requires the parent LIVE (must restore task before its comments — correctness ordering, not a hole) | Guard-fires curl RECORDED in `backend/README.md` ("Verify: pre-auth lockdown" — make task → soft_delete_task → create_task_comment → expect `no_data_found`). Direct-write-closed + RPC-still-works + read-still-works curls recorded (contacts representative) | **covered / CLOSED.** No CRITICAL/ISSUE. **SUPERSEDES the old "direct write path / policy LEFT IN PLACE (closing = #3)" clauses in the `event_comments`, `tasks` and `task_comments` write-path rows above — those direct paths are now CLOSED (the RPC is the sole write path); each of those rows has already been updated in place to reflect D36.** Two minor coverage GAPS (INFO, optional): (a) no privileged psql check that PUBLIC lost execute (anon curl can't prove it); (b) the direct-write-closed curl documents only contacts of the 5 tables |

| **task_categories table + `create/update/soft_delete_task_category` RPCs (SECURITY DEFINER, `20260715140000`, Decision 39 Slice A)** | New standalone table (id/name/color/timestamps/deleted_at), **born post-lockdown → RPC-only writes from day one** (cleaner than event_types, which shipped direct writes and closed them in D36). **Write path NOT reopened:** NO insert/update/delete policy, grant is `select` ONLY to anon/authenticated → the 3 SECURITY DEFINER RPCs are the sole write path (matches D36 posture). RLS enabled same migration; SELECT policy `using(deleted_at is null)` (archived hidden, unlike the `using(true)` comment/task tables). **RPC hygiene all present:** all 3 `security definer` + `set search_path=public`; `revoke execute … from public` + `grant … to anon,authenticated` on the EXACT typed sigs (`(text,text)`,`(uuid,text,text)`,`(uuid)`) — verified single definition each, no chain/orphan overload, no PUBLIC reopen. **No injection:** bodies are parameterized static INSERT/UPDATE with `trim(p_name)`, `p_color` passed through — NO dynamic SQL. **No hard-delete path:** `soft_delete_task_category` is `update … set deleted_at=now() where id=p_id and deleted_at is null` (no `DELETE`, no delete grant/policy); a direct REST `PATCH deleted_at` is rejected outright because this table exposes NO update grant or policy to anon (401) — distinct from the pre-lockdown event_types case where the update was granted and only the PostgREST RETURNING re-check (42501) blocked it. `update_task_category` guards `where id=p_id and deleted_at is null` + `raise no_data_found` — can't edit/resurrect a hidden/absent row, never writes `deleted_at`. **Malformed-data guard:** table CHECKs `length(trim(name))>0` and `color ~ '^#[0-9A-Fa-f]{6}$'` re-fire through the RPCs → a client CANNOT store a colour that breaks the client's `#RRGGBB` parser (the model's grey fallback is defensive-only, never exercised by stored data) | Client round-trip: Slice A Settings manager widget tests (`test/task_categories_screen_test.dart`, `test/task_category_test.dart`). **DB-layer curl RECORDED in `backend/README.md`** ("Verify: task category write RPCs", added same change set) — documents SELECT-only closure (direct `POST`/`PATCH`→401 vs RPC works), soft-delete-survives-with-`deleted_at` (privileged psql read, since `using(deleted_at is null)` hides it from anon), blank-name→400, malformed-color→400, no-delete-grant→401 | **covered** — the verify curl now lives in `backend/README.md`; attack surface is clean by construction (writes RPC-only, no hard-delete, checks fire, no injection, no PUBLIC/anon-write reopen). INFO baseline pre-auth (single-user, no login — D37): anon READs live rows, no owner-scoping. **Revisit-only-if-auth (D37):** SECURITY DEFINER bypasses RLS — if the no-auth decision is revisited, an `auth.uid()` owner check goes inside all 3 |

| **task_category_links join table + `p_categories uuid[]` on create_task/update_task (SECURITY DEFINER, `20260715150000`, Decision 40 Slice B)** | New M2M `task_category_links(task_id, category_id)` composite PK, both FKs `on delete cascade`; **verbatim mirror of `task_contacts`**. RLS on (:43), **SELECT-only** grant/policy for anon/authenticated (`using(true)` :49-51, `grant select` :54, NO write policy/grant) → membership set ONLY by the RPCs = D36 posture, **write path NOT reopened by construction**. `create_task`/`update_task` re-signatured via `drop … ; create or replace …` adding `p_categories uuid[]`; **drops (:58,:101) match the prior importance-era sigs EXACTLY** (`create_task(text,text,uuid[],smallint)`, `update_task(uuid,text,boolean,text,uuid[],smallint)` — the latest defs, confirmed) → no orphan overload keeps PUBLIC execute, no PGRST203. **D36 lockdown correctly re-honored on the NEW sigs (:167-171):** BOTH `revoke execute … from public` AND `grant … to anon,authenticated` re-issued (`…,smallint,uuid[]`). `security definer` + `set search_path=public` retained on both bodies (:69-70/:120-121). **No injection:** membership INSERT is `insert … select v_id/p_id, c from unnest(p_categories) as c on conflict do nothing` — parameterized array, NO dynamic SQL; `unnest(NULL)`/`unnest('{}')` = safe no-op; `on conflict` dedupes. **FK integrity:** `category_id references task_categories(id)` → a bad/foreign id raises FK violation and rolls back the whole atomic RPC (pre-auth "foreign" is moot — single user). `create_task` p_categories default `'{}'`; **`update_task` p_categories REQUIRED-no-default** (same footgun-guard as p_contacts/p_importance — omitted arg fails loudly PGRST202 instead of silently WIPING the set). update_task's `delete from task_category_links where task_id=p_id` (replace-the-set) is a hard-DELETE of derived join rows = annotated database.md #4 exception (mirrors update_event/task_contacts); **guarded** — the `update … deleted_at is null` + `if not found raise no_data_found` runs BEFORE the delete, so an unknown/archived task's roster can't be silently rewritten. **Soft-deleted-category → null embed:** link SELECT is `using(true)` but `task_categories` SELECT is `using(deleted_at is null)` (Slice A) → embedded to-one `task_categories(...)` comes back null for an archived category → `Task.fromJson` skips it. Cascade FKs dormant (tasks & task_categories are soft-delete-only, never hard-deleted). No write bypass: no direct write grant/policy on the link table | Client round-trip covered: `test/task_test.dart` (categories parse/embed/toRpcParams/copyWith), `test/task_form_screen_test.dart`, `test/task_detail_screen_test.dart`, `test/tasks_list_screen_test.dart`, `test/category_picker_screen_test.dart` (+14 tests this slice). Null-embed client tolerance covered by the `fromJson`-skips-null test. **DB-layer join curl NOT recorded in `backend/README.md`** — no curl documents create_task/update_task with `p_categories` inserting/replacing `task_category_links`, the `on conflict` dedupe, the FK-violation-rolls-back on a bad category_id, or the `tasks?select=…,task_category_links(category_id,task_categories(id,name,color))` embed reading back (incl. the soft-deleted-category→null embed at the DB layer) | **covered** (client) / DB-curl **gap** (same pattern as the `task_contacts` row — client tests exist, no `backend/README.md` join curl). Attack surface clean by construction: writes RPC-only (not reopened), no hard-delete of soft-deletable entities, no injection, FK enforced, D36 revoke+grant re-issued on exact sigs, search_path pinned. INFO baseline pre-auth (single-user, no login — D37): anon READs links + embedded categories, no owner-scoping. **PHASE-FLIP WATCH (revisit-only-if-auth, D37):** twin of the task_contacts item — `p_categories` links ANY existing category and the roster embeds `task_categories(id,name,color)` under `using(true)`; if the no-auth decision is revisited, create/update_task MUST validate each `p_categories` id belongs to `auth.uid()` and the link SELECT must gate on the parent task's owner (else cross-tenant read/link). Categories are lower-sensitivity than contacts (no PII), so the disclosure risk is milder than the task_contacts twin |

| **Idempotent create_* — client-supplied `p_id` on all 7 create RPCs (`20260716120000`, Decision 41 / issue #9)** | DROP+recreate all 7 create_* adding trailing `p_id uuid default null`; body `v_id := coalesce(p_id, gen_random_uuid())`, explicit-id INSERT, `on conflict (id) do nothing`. Client mints the id (`lib/util/ids.dart` v4 uuid) and reuses it across re-taps. **Re-lock verdict: CLEAN — all 7 re-locked correctly.** Each recreated RPC re-issues BOTH `revoke execute … from public` AND `grant … to anon,authenticated` on the EXACT new sig (contact `(…,uuid)`, event `(…,uuid,uuid)` two trailing uuids, event_type/task_category `(text,text,uuid)`, task `(…,smallint,uuid[],uuid)`, comment/task_comment `(uuid,text,uuid)`) → no RPC left with default PUBLIC execute (D38 recreate invariant honored). **DROPs use exact prior sigs** (`create_contact(text,date,text,text,text,text)`, `create_event(…,uuid[],uuid)`, `create_task(text,text,uuid[],smallint,uuid[])` = latest post-categories, `create_task_comment(uuid,text)`, etc.) → no orphan overload keeps PUBLIC execute, no PGRST203 ambiguity. `security definer` + `set search_path=public` retained on all 7 (identical to priors — no regression; SET search_path='' remains the deferred optional #3 slice). Ends `notify pgrst,'reload schema'`. **`p_id` as attack surface — bounded, no escalation:** `on conflict (id) do nothing` NEVER updates → a caller supplying an EXISTING id cannot overwrite/hijack/corrupt that row; the create silently no-ops and the repo's `_fetchOne(v_id)` returns the pre-existing row. Worst case = existence-probe / wrong-row-returned, which is ≤ what anon SELECT already grants (single-user, no owners to cross). **`create_task_comment` guard HOLDS:** insert-select gated `where exists(tasks live)` `on conflict do nothing`, then `if not exists(row v_id) raise no_data_found`. Archived parent + fresh id → no insert → raises (guard fires). Archived parent + id colliding with an existing comment → no insert (WHERE false) but post-check finds the pre-existing row → returns success WITHOUT writing — a false-SUCCESS replay-collision nuance, but the security intent (no comment added to an archived task) is INTACT (nothing written); body discarded, no mutation of the colliding row | Client round-trip: repo `_fetchOne` return-existing-or-new tests; `lib/util/ids.dart` + `test/util/ids_test.dart`. **DB-layer curls NOT yet recorded in `backend/README.md`** — recommend: (1) privileged psql `has_function_privilege('public','public.create_contact(text,date,text,text,text,text,uuid)','execute')`=`f` (+ anon=`t`) on a representative recreated sig, proving the recreate didn't reopen PUBLIC; (2) same-`p_id`-retry idempotency curl: call `create_contact` twice with identical `p_id` → one row, 2nd is no-op, `_fetchOne` returns it; existing-id no-overwrite curl (supply an id whose row differs → row unchanged, no 2nd row); (3) `create_task_comment` guard curl: archived parent + fresh id → `no_data_found`; and document the replay-collision false-success (archived parent + existing comment id → returns, no write) | **covered by construction / DB-curl gap (INFO).** No CRITICAL/ISSUE — no D36/D38 regression: re-lock complete on all 7, no PUBLIC reopen, no new anon/authenticated direct-write path (writes stay RPC-only), search_path/security-definer unchanged, `on conflict do nothing` precludes row-hijack. INFO baseline pre-auth (single-user, no login — D37): `p_id` existence-probe / wrong-row-return ≤ anon SELECT. **Minor deploy note (INFO):** the file DROPs+recreates then revokes at the end; if `deploy-homebase.sh` runs it un-wrapped (autocommit per statement) there is a transient window where recreated RPCs have default PUBLIC execute before the trailing revoke — negligible on a single-user tailnet box, but wrapping the migration in `begin;`/`commit;` would close it. **Revisit-only-if-auth (D37):** if the no-auth decision is revisited, client-chosen `p_id` becomes a real concern — a caller could probe/target another user's row ids; then create/update RPCs must owner-scope AND ignore/validate `p_id` |

## Notes
- **Recommended coverage to close the two minor gaps (INFO, not blocking):**
  (a) **no-PUBLIC-execute privileged check** — on homebase as postgres: `psql -c "select has_function_privilege('public','public.create_task(text,text,uuid[])','execute');"` should return `f` for a representative RPC (an anon curl cannot prove this — anon calls via its own grant). (b) **direct-write-closed on the other 4 tables** — clone the contacts direct-`POST`/`PATCH`→401/403 curl for event_types, event_comments, tasks, task_comments (esp. the two comment tables that previously had `using(true)` + both write paths open) so the closure is documented per-table, not just for contacts.
- **No auth is planned (Decision 37)** — `cross-user data access` and `owner-scoping` stay
  `N/A (no auth, D37)`. Only IF the no-auth decision is ever revisited (the CRM is shared with another
  person, exposed beyond the tailnet, or made multi-tenant) do they flip to required checks (recommend
  an integration/curl check that user A can't reach user B's rows).
